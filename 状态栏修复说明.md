# 状态栏为什么一直不对，这次到底改了什么

## 先说结论

前几次我都修错了地方。真正的原因是：

**现在的角色卡，状态栏塞进来的不是一小段 `<div>`，而是一整份完整的 HTML 网页。**

你给我的 8 张卡，51 条正则脚本，我一条条跑过一遍——
其中 30 多条的 `replaceString` 是从 `<!DOCTYPE html>` 开头、带 `<head>`、带几万字符
`<style>`、还带 `<script>` 做交互的**完整网页**。最大的一条（沉沦法则的手账本）
17 万字符。

谷雨之前是把这份网页直接 `innerHTML` 塞进消息气泡里的。四件事同时炸：

| 出的问题 | 你看到的现象 |
|---|---|
| `innerHTML` 会丢掉 `<!DOCTYPE>`/`<html>`/`<head>`/`<body>`，反而把 `<meta>`/`<title>` 漏进正文 | 卡片旁边有一截莫名其妙的怪文字 |
| `innerHTML` 塞进去的 `<script>` **永远不会执行** | 点展开没反应、切页签没反应、进度条不动——卡片是死的 |
| 卡片的 `<style>` 是按"我就是整个页面"写的，全是 `body{}`、`*{}` 这种全局规则 | 卡片被谷雨的样式压住塌成一条；同时谷雨自己的字体/背景被卡片改掉 |
| 几张卡同时出现时全局样式互相打架 | 时好时坏，完全随机 |

## 酒馆是怎么做的

我按你给的两个仓库看了一遍。**酒馆助手（JS-Slash-Runner）从来就不是直接塞进页面的**——
它把每一个"看起来像完整前端页面"的内容**单独丢进一个 iframe** 渲染
（`src/panel/render/iframe.ts` + `src/util/is_frontend.ts`）。

iframe 自带文档边界：样式进不来也出不去，`<script>` 正常执行，高度在里面量好了报给外面。

这次就照抄了同一套做法，连判定标准都跟它保持一致：内容里出现
`html>`、`<head>`、`<body` 任意一个，就当完整前端页面走 iframe；
只是一小段 `<div>` 的（比如霍樊那张）继续原地渲染，不动它。

## 具体改了这几处

**`js\03-markdown-feed-tags.js`**（主要改动）

- 新增 `isFrontendHtml()` / `makeFrontendPlaceholder()` / `mountFrontendFrames()`：
  完整网页改成 iframe 渲染。用 MutationObserver 兜底，所以**所有渲染入口
  （续写、小说章节、沉浸阅读、推文、日记、评论、论坛）都不用改，自动都能用上**。
- iframe 里垫了三样东西：
  - **高度自动上报**（ResizeObserver + postMessage）。卡片展开/收起时外面跟着长高缩回。
    量高度**只量 body**——之前算上 `documentElement` 会形成回路，高度只增不减，
    卡死在初始值上，表现就是"卡片只露出一小条"。
    脱离文档流的元素（`position:absolute/fixed`，手账本那种整屏界面全靠它）
    不计入 `scrollHeight`，单独量了一遍补上。
  - **localStorage 垫片**。`file://` 下（桌面版 exe、HBuilderX 打的 APK）iframe 拿到的是
    不透明源，卡片一碰 localStorage 就抛异常当场挂掉。现在能用就用真的，不能用退回内存版。
  - **jQuery**（新增 `js\vendor\jquery.min.js`）。只在卡片确实要用、而且**它自己没定义 `$`**
    的时候才挂——有些卡自己写了个极简 `$` 然后拿返回值当原生 DOM 用，硬塞 jQuery 反而把
    好好的卡片弄坏。
- **代码围栏改成最先处理，处理完立刻保护起来**。以前排在最后，卡片刚替换进来的
  HTML 会先被一堆 markdown 规则（`**加粗**`、`*斜体*`、`[标签|值]`、`<同名标签>`）啃一遍——
  卡片 HTML 里到处是 `*`、`[ ]`、`<div>xx：yy</div>`，几乎必然被误伤：style 属性从中间拆开、
  结构被塞进多余的 span、整块被当成"通用状态块"重新包装。

**`js\01-core-state-infra.js`**

- `applyDisplayOnlyRegex` 现在认 `minDepth`/`maxDepth` 了。卡片作者用得很多：
  高山仰止那张报纸状态栏是 `maxDepth=0`（**只在最新一楼**渲染）、蔚野的仿IG是 `maxDepth=1`、
  沉沦法则的手账本是 `maxDepth=3`。之前完全没看，等于每一楼都渲染一份十几万字符的卡片，
  翻旧消息会卡到动不了。

**`js\16-story-studio.js`** / **`js\08-diary-novel.js`** — 渲染时把楼层深度传下去。

**`style.css`** — `.gy-frontend-frame` 样式。

## 修完的效果

51 条脚本全部跑通，0 条有问题（之前 32 条有问题）：

- 完整网页类卡片：全部进 iframe，**零样式泄漏、零标签残留**，高度从 94px 到 4289px 按内容自动撑开
- 片段类卡片（霍樊状态栏、蔚野的聊天气泡那一套）：继续原地渲染，不受影响
- 删除类脚本（清屏、抹除占位符）：正常删干净

闻述那张卡的测试里新增了两条以前根本测不到的：
**「卡片自带的 `<script>` 真的执行了」**、**「展开后 iframe 自动跟着长高」**——
这两件事以前从来没成功过。

测试：`card-diag 51/51`、`card-test 13/13`、`run 102/102`、`boot 9/9`。

新增了 `_test\card-diag.mjs`：把角色卡目录里所有卡的所有正则脚本挨个跑一遍渲染链路，
报告每一条渲染成什么样、有没有泄漏/残留/撑不开。以后再遇到新卡有问题，直接跑它。

## 还需要你手动删的

我这边删不掉你电脑上的文件，这几个之前提过的还留着：

- 旧的 `manifest.json`（已改名 `pwa-manifest.json`，旧的留着会跟 HBuilderX 撞车）
- `_酒馆续写\` 文件夹（如果还在）
- 启动页里没标尺寸的那批旧文件名
- `谷雨桌面版打包工具\` 和它的 zip

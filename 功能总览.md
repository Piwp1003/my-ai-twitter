# 谷雨 · 功能总览

> 整理日期：2026-08-26（含当日修复）
> 一个纯前端的 AI 角色扮演 / 陪伴应用，界面仿 Twitter，全部数据存在本机 IndexedDB，不上传服务器。
> 规模：`index.html` + 17 个 JS 模块，约 1.5 MB、838 个函数、13 个主视图、29 个弹窗。

---

## 目录

1. [社交主线：推文 / 评论 / 通知](#一社交主线)
2. [聊天](#二聊天)
3. [角色](#三角色)
4. [写作：日记 · 信件 · 小说 · 续写](#四写作)
5. [论坛与营销号](#五论坛与营销号)
6. [记忆系统](#六记忆系统)
7. [世界书](#七世界书)
8. [预设与提示词工程](#八预设与提示词工程)
9. [插件与小游戏](#九插件与小游戏)
10. [酒馆（SillyTavern）兼容](#十酒馆兼容)
11. [导入导出与云同步](#十一导入导出与云同步)
12. [界面与个性化](#十二界面与个性化)
13. [AI 接口与生成控制](#十三ai-接口与生成控制)
14. [移动端与 APK](#十四移动端与-apk)

---

## 一、社交主线

### 发推
- **手动发推** —— 自己写一条推文发出去
- **选择发帖身份** —— 可以用「我」的身份发，也可以用任意一个角色的身份发，头像会跟着变
- **高级设置** —— 手动指定地点 / 转发数 / 浏览量 / 评论数 / 点赞数，留空则由 AI 自动编
- **配图** —— 上传图片自动转 base64 存储，过大的图自动压缩（小 GIF 除外，保留动图）
- **引用推文** —— 引用别人的推文再发表评论，原帖被删会显示"已被删除"占位
- **悬浮发帖按钮** —— 可自由拖到屏幕任意位置并记住，未拖动的点击视为"打开发帖框"
- **召唤角色发帖** —— 指定某个角色（或随机）立刻生成一条推文
- **自动发帖** —— 后台定时让角色自己发推

### 推文互动
- 点赞 / 转发 / 评论 / 引用 / 收藏
- **分享到聊天** —— 把推文以引用消息形式塞进跟某个角色的聊天框
- **双击正文直接编辑**
- **发帖后全体角色自动反应** —— 点赞、公开评论、或转成私聊来找你；高冷人设也会有基本反应，但与你有情感羁绊的高冷角色不再被锁死为"只能点赞"

### 评论区
- **楼中楼** —— 子回复默认显示前 2 条，其余折叠进"显示更多回复"
- **评论区活起来** —— 角色之间会互相接话、吵架
- **路人 NPC** —— 有自己身份设定的路人来评论
- **评论转私聊** —— 角色评论着评论着就转到私信找你（可关）
- **评论支持表情包和图片**

### 话题与趋势
- `#标签` / `@提及` 自动高亮成可点击的链接
- **趋势榜** —— 手动添加 / 编辑 / 删除话题
- **角色自创话题自动上榜** —— 同一标签被 ≥3 个不同角色用过就自动收录进趋势
- **给 AI 喂热门标签** —— 让新发帖的角色优先沿用已有标签，话题才真的能"火"起来

### 通知
- 通知列表 + 未读角标（顶部和底部导航两处同步）
- 点通知直接跳到对应帖子 / 聊天
- **应用内滑动通知条** —— 顶部弹出、可点击跳转、可上滑关闭
- **系统级推送** —— 页面不可见时通过 APK 原生推送 / Service Worker / 浏览器通知三重兜底

---

## 二、聊天

### 联系人列表
- **两种外观** —— 横向头像条 / 竖排列表，随时切换
- **排序规则** —— 未读优先 → 最后消息时间倒序，置顶的永远在最前
- 长按/右键置顶、取消置顶
- 未读红点、分支标记、最后一条消息预览

### 消息收发
- **两种回复模式**（设置 →「💬 聊天回复条数/长度模式」，随时可切）
  - *可控字数模式*（默认）：条数在「最少~最多」范围内 AI 自己定，几条加起来不超过「聊天回复总字数」
  - *经典模式*（旧版聊天体验）：条数随机 1~4 条，单条不限字数，不受总字数限制
  - 两种模式在**正常聊天、角色主动发消息、侧滑重新生成**三条路径上行为一致，不会中途串档
- **流式输出** —— 打字机效果，可中途取消；多段回复中先写完的气泡会提前显示
- **已读/未读回执**
- **引用回复** —— 长按消息引用它
- **发图识图** —— 发图片给角色，角色能看懂
- **表情包回复** —— 角色会挑表情包回你
- **导演耳语** —— 偷偷给 AI 下指令，不作为聊天内容
- **连续发送合并触发** —— 你连发几条，AI 一次性统一回复
- **留空点发送** —— 让角色针对你最后几条消息重新回一次
- **拍一拍** —— 双击头像，角色可能反击
- **语音输入**（麦克风转文字）与**语音朗读**（自动去掉 Markdown/HTML 符号）

### 消息管理
- 编辑我的消息（会截断后面并重新生成）
- 编辑角色消息（同步更新引用、相册、向量记忆）
- 删除消息 / 收藏进相册
- **侧滑重新生成** —— 不满意就换一版
- **侧滑切换版本** —— 在生成过的多个版本间翻页
- **开辟对话分支** —— 从某条消息岔出一条新线，随时切回来
- **历史聊天归档** —— 查看/删除/一键恢复某段历史记录；恢复时会把当前对话自动存进原档案位（双向互换，不丢数据）
- 导出聊天记录为 TXT / 从 TXT 导入

### 群聊
- 创建群聊，选角色、设群名群头像
- **发言顺序** —— 全部发言 / 仅 @ 到的人 / 按顺序轮流 / 随机
- **禁言**
- **群聊转私聊** —— 群里聊着聊着私信你（可关）

### 开场白
- 新聊天自动弹出候选开场白供选择
- 支持"不使用开场白"，会识别并标红"目录页"型开场白
- 续写模式可汇总多个角色的开场白

---

## 三、角色

### 角色管理
- 角色中心：新建 / 编辑 / 删除 / 关注 / 特别关注
- 人设、头像、背景图、认证标、简介、性别、所属势力
- **角色卡导入** —— 支持 SillyTavern 的 JSON 和 PNG 角色卡

### 角色的"生活"
- **日程系统** —— AI 生成角色一整天的行程，可预览确认后保存
- **状态气泡** —— 结合日程和真实时间生成"此刻在做什么"，后台每 15 分钟自动更新
- **日程忙碌联动** —— 角色在忙的时候回复会变慢或被打断
- **后台自动小剧场** —— 每 5 分钟按 30% 概率抽一对有关系的角色，生成他们背着你发生的私下互动，可选弹窗告诉你
- **角色间私下互动开关** / 互动后提醒开关 / 回复延迟开关

### 关系网
- **势力分组** —— 给角色分派系
- **角色关系图** —— 可视化谁跟谁什么关系
- **关系驱动的主动介入** —— 推文/爆料/论坛提到某人时，跟他有关系的角色会自己决定要不要来插一嘴
- 关系上下文注入 —— 让角色"记得"自己跟你、跟其他角色的关系

### 纪念日
- 用户自定义纪念日（增删）
- 角色专属纪念日 + 回忆相册
- **认识里程碑自动播报** —— 1/7/30/100/200 天等节点在聊天里自动提示（每天只提一次）
- **AI 生成纪念寄语** —— 结合认识天数和已收藏的高光回忆写一段话

---

## 四、写作

### 日记与信件
- 角色写日记 / 写信给你
- 查看、导出为 TXT
- 内容纳入全站搜索和语义检索

### 小说
- AI 生成故事章节
- **用已有内容生成小说** —— 从续写 / 日记信件 / 聊天 / 推文 / 评论 / 记忆里自选素材，AI 总结改写成一章
- 右键导航栏「我们的故事」可自定义美化 UI（自定义 CSS）
- 导出 TXT

### 续写工作台（Story Studio）
- 独立的多轮互动续写界面
- 会话管理：新建 / 打开 / 重命名 / 删除
- 参与角色勾选
- 每轮可编辑、重新生成
- 独立的系统提示词构建和历史裁剪
- **单独的思维链展示开关** —— 续写场景可以看到 AI 的思考过程，其它场景一律剥离

---

## 五、论坛与营销号

### 匿名论坛
- 匿名发帖 / 跟帖，你有自己的匿名马甲身份
- 楼中楼树状回复
- **专属角色被 @ 时会来回复**
- **NPC 自动发言** —— AI 生成普通路人的论坛言论

### 营销号 / 小报
- NPC 小报爆料引擎，会编排你和角色之间的"修罗场"
- 详情页点赞、删除
- 爆料内容纳入全站搜索

---

## 六、记忆系统

### 向量记忆（语义检索）
- 后台自动为消息算向量，检索"语义相关但不在最近上下文里"的历史内容
- 覆盖范围：聊天消息、日记、信件、小说章节、续写、论坛发帖跟帖、匿名论坛发言
- 可配独立的 Embedding 模型和专用 API

### 角色专属资料库（RAG）
- 给角色上传 `.txt` / `.docx` 文档
- 自动切分成片段并计算向量
- 对话时自动检索相关片段作为参考

### 记忆总览
- 独立页面，汇总全部记忆
- **记忆召回面板** —— 识别 `<recall>` / `<supplement>` 标签，展示本轮引用了哪些记忆
- 记忆库手动增删条目

### 回忆相册
- 收藏推文 / 聊天消息
- 个人主页显示全部收藏，角色主页只显示与该角色相关的
- 点卡片跳回原文

### 自动总结
- 单聊 / 群聊 / 推文各自可配触发频率

---

## 七、世界书

- 条目增删改查，支持分类标签管理
- **匹配引擎仿 SillyTavern World Info** —— 关键词 / 次要关键词 / 触发概率 / 黏着轮数 / 冷却轮数 / 延迟轮数 / 权重排序
- **全局条目 vs 角色专属条目**
- **深度注入** —— 插进聊天历史的指定位置，而不是堆在系统提示词里
- **上下文预算控制** —— 限制世界书最多占多少 token
- **导入** —— 从 TXT 或 SillyTavern 世界书 JSON 批量导入，导入前逐条预览编辑，可单条删除

---

## 八、预设与提示词工程

- **仿 SillyTavern Chat Completion 预设管理界面**
- 新建 / 重命名 / 复制 / 删除 / 设为当前使用（互斥单选）
- **提示词模块** —— 增删改、启用禁用、**拖拽排序**
- **深度注入** —— 模块可插到聊天历史"倒数第 N 条"之前
- **插入位置** —— 人设之前 / 人设之后 / 提示词末尾
- **采样参数** —— 温度 / Top P / Top K / 频率惩罚 / 存在惩罚，跟着预设走
- **小剧场模块** —— 名称含"剧场"二字的模块被单独识别和管理
- **导入导出** —— 导出为 SillyTavern 可识别的 JSON；导入时逐条预览、勾选启用、不适配的模块会标注原因但仍原样导入不丢失，可一并导入配套正则脚本

### 宏系统
- **变量宏**：`{{getvar}}` `{{setvar}}` `{{addvar}}` `{{incvar}}` `{{decvar}}` `{{getglobalvar}}` `{{setglobalvar}}`，支持点号路径读写嵌套对象
- **文本宏**：`{{char}}` `{{user}}` `{{time}}` `{{date}}` `{{weekday}}` `{{random:}}` `{{roll:}}` `{{//注释}}`
- **EJS 模板** —— 角色卡/世界书里可写 `<% %>` 逻辑代码

### 正则替换脚本
- 增删改查，作用对象可选（AI 回复 / 我的消息 / 双向）
- 是否用正则、是否只影响显示、是否只影响发给 AI 的内容
- 按楼层深度限定生效范围
- 可绑定专属角色

### 人味强化
- **时间感知模块** + **TPES 时间感知增强系统**
- **人味强化协议** —— 反套路 / 反回声 / 情绪校准
- **Token 预算估算** —— 仿酒馆顶部的"当前 prompt 大小"提示

---

## 九、插件与小游戏

### 插件系统
四种类型，可导入导出分享：

| 类型 | 作用 |
|---|---|
| 📜 提示词规则 | 往 prompt 注入固定指令，可全局或指定角色 |
| ⚡ 快捷动作 | 聊天框上方加按钮，点一下发预设指令（掷骰子、查天气……） |
| 🔤 宏 | 扩展 `{{xxx}}` 占位符 |
| 🧬 脚本钩子 | 自定义 JS，权限跟手写代码一样大 |

脚本钩子有三个切入点：
1. **生成前** —— 往 prompt 注入内容
2. **生成后** —— 处理 AI 返回的 JSON（好感度、打断回复等）
3. **网页打开时** —— 自动执行一次，可自己往页面里插按钮/新页面/弹窗/样式，实现"导入即用"

- 单条启用/禁用，作用域可选全局或多个指定角色
- 导入后立刻执行启动钩子，**不用刷新**
- **🔄 刷新键**（2026-08-26 新增）—— 手机顶栏图标 / 电脑侧边栏 / 插件页三处都能点

### 小游戏框架
- 游戏由插件通过 `registerMiniGame()` 注册进来
- 可拖动的浮动游戏面板
- 游戏中角色发言开关

---

## 十、酒馆兼容

- **TavernHelper 兼容层** —— 模拟 SillyTavern 助手的上百个接口：读写变量、读写聊天记录、世界书、正则、预设、人设、音频、脚本按钮、事件监听
- **未实现接口安全兜底** —— 161 个接口里没实现的自动生成空实现，防止卡片脚本整体崩掉
- **角色卡前端界面 iframe 隔离渲染** —— 整份 HTML 页面放进独立 iframe，样式脚本互不污染
- **iframe 内按需注入 jQuery**（卡片用到 `$` 且没自定义时才注入）
- **iframe 内 localStorage 兼容垫层**（`file://` 下自动降级为内存版）
- **vh 单位自适应转换** + **高度自适应上报**
- **MVU 变量状态栏** —— 识别 `<UpdateVariable>` 补丁块，渲染成正负变化上色的状态卡
- **通用状态栏识别** —— `[标签|值1|值2]` 和 `<标签>字段：值</标签>` 两种写法都能自动转成状态条
- **状态栏代码围栏自动解包**

### 思维链处理
- 识别多种格式：`think` / `thinking` / `details` / `custom_think` / `SECRET`，可自定义增删启停
- 三种展示模式：折叠展示 / 直接剥离 / 原样保留
- 续写场景可单独开启展示，其它场景一律剥离

---

## 十一、导入导出与云同步

- **全量备份** —— 导出/导入整个存档 JSON（角色、聊天、设置全都在）
- **从网络链接一键导入** —— 粘贴 GitHub / Gitee / CDN 直链，自动转成 raw 链接并识别内容类型（预设 / 角色卡 JSON / 角色卡 PNG / 世界书 / 正则脚本），调对应导入逻辑
- **聊天记录导入** —— 支持 SillyTavern 的 `.jsonl`，自动清洗思维链和状态栏补丁，多种时间格式兜底解析
- **云端主动消息** —— 配合 Cloudflare Worker，页面关着时也能收到角色的主动消息；打开时自动拉回云端攒的内容
- **多用户人设（马甲）** —— 把当前身份存成命名快照，随时切换/重命名/删除
- **PWA 离线缓存** —— Service Worker 缓存全部核心文件，断网也能打开

---

## 十二、界面与个性化

- **深色模式**
- **全局自定义 CSS** —— 想怎么改外观都行
- **全局背景图** + 不透明度调节
- **自定义站点 Logo** 和署名
- **液态玻璃猫猫主题**（`liquid-glass-cat-theme.css`）
- **点击特效** —— 爪印 / 爱心 / 星星 / 自定义图片，点哪冒哪
- **表情包库** —— 批量上传、删除、编辑含义描述
- **状态气泡类型** —— 自定义标签的名称/颜色/透明度，带调色盘
- **图片裁剪器** —— 上传头像背景时可缩放拖拽按比例裁
- **悬浮式滚动条** —— 平时隐藏，滚动时才淡入
- **拖拽调高度手柄** —— 文本框底部拖着调大小，触屏也能用
- **相对时间显示** —— "刚刚 / x分钟前 / x天前"，每分钟自动刷新
- **数字简写** —— 1.2k / 1.2万
- **Esc 关弹窗** —— 输入框有内容时先失焦，不误关
- **输入框看门狗** —— 每 2 秒检测聊天输入框有没有被残留浮层挡住，自动收拾

---

## 十三、AI 接口与生成控制

- **三套独立 API 配置** —— 主 API / 副 API / 向量记忆专用 API
- 自动识别并适配 OpenAI 兼容接口和 Anthropic 原生接口
- **模型不可用自动兜底** —— 遇到"模型不存在/未开通"自动切回上次可用的模型重试，并同步更新设置
- **限流自动重试** —— 并发超限时自动等待重试，不直接报错
- **CORS 错误人话提示**
- **JSON 解析容错** —— 自动修复模型返回的不规范 JSON（多余逗号、未转义换行引号），避免整段代码糊在聊天里
- **JSON 信封自动拆解** —— 整段消息其实是没解析成功的 `{"replies":...}` 时自动拆出文字，并修复历史里已经存坏的旧消息
- **残留标记清理** —— 清掉 `[QUOTE:N]` `[MOVETOCHAT]` `[NUDGE]` `[EMO:xxx]` 这些内部标记
- **聊天/论坛强制纯文本** —— 屏蔽角色卡和正则可能带来的 HTML
- **字数上限**分别可配：聊天 / 推文 / 日记 / 信件 / 评论
- **上下文控制** —— 聊天历史带入轮数、世界书预算、语义检索预算
- **休息时间段** —— 设定角色的作息，这段时间不主动找你

---

## 十四、移动端与 APK

- **响应式布局** —— 电脑左侧导航栏，手机汉堡抽屉 + 底部导航 + 顶栏（顶栏中间显示当前页面名，右侧搜索 / 🔄刷新 / 设置三个图标）
- **打包成 APK**（HBuilderX 5+App）
  - 启动自检面板：白屏时把报错/文件名/行号直接画在屏幕上，12 秒超时兜底
  - 第三方库全部本地化，断网也能启动
  - 原生网络通道（`plus.net.XMLHttpRequest`），失败自动回退 fetch
  - 原生弹窗替换浏览器 alert
  - 安卓存储/相机权限自动申请
  - 文件保存写进手机文档目录
  - 键盘弹出时界面自动收缩（`visualViewport` 实时算高度）
  - 原生弹窗关闭后自动找回输入焦点
- **桌面版 exe**（`桌面版/` 目录 + `启动谷雨(桌面模式).bat`）
- **PWA** —— 可"添加到主屏幕"当 App 用

---

## 附：文件结构

| 文件 | 负责 |
|---|---|
| `index.html` | 全部页面骨架、13 个视图、29 个弹窗、启动自检 |
| `js/01-core-state-infra.js` | 核心状态、API 调用、宏系统、正则脚本、思维链、向量记忆 |
| `js/02-databank-plugins-minigames.js` | 插件系统、小游戏框架、刷新键 |
| `js/03-markdown-feed-tags.js` | Markdown 渲染、角色卡 iframe、推文卡片、趋势、启动流程 |
| `js/04-mobile-bootstrap-notifications.js` | 移动端 UI、通知、存档读写、设置面板 |
| `js/05-anniversary-memory.js` | 纪念日、联系人列表、聊天消息渲染与收发 |
| `js/06-*-worldbook.js` | 表情包、时间感知、人味协议、关系网、世界书引擎、记忆总览 |
| `js/07-proactive-cloudsync.js` | 主动发消息、云端同步、视图切换 |
| `js/08-diary-novel.js` | 日记信件、小说生成 |
| `js/09-memories-search-postfixes.js` | 回忆相册、全站搜索、推文详情、发推 |
| `js/10-comments-npc-forum-reply.js` | 评论区、路人 NPC、角色列表 |
| `js/11-tabloid-story-forum-engine.js` | 营销号小报、论坛引擎 |
| `js/12-worldbook-import-appexport.js` | 世界书导入、文件导出、自绘弹窗、链接导入 |
| `js/13-*-cardimport.js` | 角色回复、群聊、势力关系网、角色卡导入 |
| `js/14-settings-archive.js` | 互动设置、后台小剧场、历史聊天归档 |
| `js/15-ai-presets.js` | 预设管理 |
| `js/16-story-studio.js` | 续写工作台 |
| `js/17-tavern-bridge.js` | 酒馆助手兼容层（主页面侧） |

---

## 附：2026-08-26 这天修掉的

| 问题 | 现在 |
|---|---|
| `manifest.json` 撞 HBuilderX 配置导致 APK 白屏 | 已删除，打包不再白屏 |
| 「以角色身份发帖」下拉框缺失，功能静默失效 | 补回，可用任意角色身份发推 |
| 启动中途报错会让每个后续错误都糊全屏「启动失败」 | 启动标志改到 `finally` 里 |
| 手机顶栏页面标题永远不显示 | id 改对 + 解除 CSS 强制隐藏，正常显示 |
| 手机搜索框被压成 28px 没法打字 | 顶栏布局分开设置，宽度正常 |
| 搜索结果里「信件」被标成「日记」 | 单复数改对 |
| 发帖时填 0 赞会被随机数覆盖 | 显式判空，0 保留 |
| 「未分类」筛选下导入世界书会污染分类列表 | 排除哨兵值 |
| 带撇号的话题标签会撑坏 HTML | 补 `escapeAttr` / `escapeJsArg` 两级转义 |
| 经典模式下侧滑重新生成会退回可控字数模式 | 三条聊天路径统一走同一个模式判断 |
| Service Worker 漏缓存 `localforage`，离线打不开 | 补齐，21 个资源全覆盖 |
| 插件导入后要大退 App 才生效 | 新增 🔄 刷新键（顶栏 / 侧边栏 / 插件页） |

---

*由 林 制作 · 本文档由 Claude 整理*

# 谷雨 · 代码体检报告

> 体检日期：2026-08-26　｜　体检范围：`index.html`、`js/01`~`js/17`、`service-worker.js`、`style.css`、全部说明文档
> 代码量：约 1.5 MB、838 个函数、618 个元素 id

---

## 零、先看这一条

### 🔴 `manifest.json` 还躺在项目根目录，没删

`打包APK说明.md` 的最后一行写着：

> 老的 `manifest.json` 我这边删不掉，**你自己删掉它**，免得下次拷进 HBuilderX 又撞车。

**这个文件现在还在。** 而且经过比对，它和 `pwa-manifest.json` **内容一字不差**——就是当初改名前的那一份。

这意味着：只要你把项目文件夹整个拷进 HBuilderX 的 5+App 项目，它就会再次覆盖掉 HBuilderX 自己的 `manifest.json`（appid / 入口页 / 权限 / 启动图全在里面），**打出来的 APK 还是白屏**，和上次一模一样。

**处理办法（三选一）：**

1. 直接删掉 `D:\MyTwitterAI\克劳德神了\manifest.json`（推荐，代码里已经没有任何地方引用它了）
2. 或者改名成 `manifest.json.bak`
3. 或者拷文件进 HBuilderX 时手动跳过它（不推荐，早晚会忘）

已核实：`index.html` 第 20-25 行挂的是 `./pwa-manifest.json`，`service-worker.js` 的预缓存清单我这次也改成了 `./pwa-manifest.json`。删掉 `manifest.json` **不会影响任何功能**。

---

## 一、APK 打包适配情况

### ✅ 做得好的部分（这些都已经到位，不用再动）

| 项目 | 状态 | 说明 |
|---|---|---|
| 启动自检面板 | ✅ 很扎实 | `index.html:27-99`。白屏时会把报错、文件名、行号直接画到屏幕上；12 秒超时兜底还会主动检查 `localforage` 有没有加载上。而且专门收窄过触发条件——只有 `<script>` 和样式表 `<link>` 挂了才算启动失败，一张远程图片加载失败不会再糊满屏 |
| 第三方库本地化 | ✅ | `localforage` / `mammoth` / `ejs` 都在 `js/vendor/` 里跟着打包，断网也能启动 |
| 原生网络通道 | ✅ | `js/01:769` 起，检测到 `window.plus` 就走 `plus.net.XMLHttpRequest`，失败自动回退普通 `fetch`，两条路都试 |
| 原生弹窗替换 | ✅ | `js/01:108`，App 环境下 `alert` 自动改用 `plus.nativeUI.alert`，解决安卓 WebView 里"点了没反应" |
| 安卓权限申请 | ✅ | `js/04:273/292`，打开文件选择器前先申请存储/相机权限，且 App 启动时会预先整体申请一次 |
| 键盘顶起适配 | ✅ | `js/01`，用 `visualViewport` 实时算出真实可视高度写进 `--app-vh`，代替写死的 `100vh` |
| 弹窗后焦点找回 | ✅ | 原生弹窗关闭后自动把输入焦点抢回来，不用切出切入 |
| 文件保存 | ✅ | `js/12`，APK 环境下写进手机文档目录，失败会换目录重试并给出原因 |

### ⚠️ 一个风险点

**`window.__guyuBooted = true` 是 `window.onload` 的最后一行**（`js/03:1425`）。

在它前面有二十多句初始化：加载数据、渲染帖子、启动定时器、云同步、初始化移动端 UI、跑插件启动钩子……

**只要其中任何一句抛异常，这个标志位就永远置不上。** 后果有两层：

1. 12 秒后必定弹出"谷雨启动失败 / 启动超时"面板，哪怕 App 其实已经能用了
2. 更麻烦的是 `bootDone()` 会永远返回 `false`，于是**此后每一个运行时错误**（包括角色卡自带脚本报的错）都会糊出一个全屏"启动失败"面板——而这恰恰是注释里写明要避免的情况

**建议改法**（把置位挪进 `finally`）：

```js
window.onload = async function () {
    try {
        // ……原有的全部初始化语句……
    } finally {
        window.__guyuBooted = true;  // 不管中间有没有出岔子，都算"启动阶段结束了"
    }
};
```

这样即使某一句失败，报错面板照样会弹（那是 `window.onerror` 干的，独立于这个标志），但不会连累后面所有错误都糊全屏。

---

## 二、确认的代码问题

按严重程度排序。每条都已回到源码核实过。

---

### 🔴 1. "以角色身份发帖"功能彻底失效

**位置：** `js/09-memories-search-postfixes.js:420`、`js/04-mobile-bootstrap-notifications.js:143` 和 `:158`

**问题：** 这三处都在找一个叫 `userPostRoleSelect` 的下拉框，但 **`index.html` 里根本没有这个 id**（两种引号写法都搜过了）。发帖弹窗里真实存在的是 `aiPostCharSelect`，那是"召唤哪个 AI 角色自动发帖"，跟"我以谁的身份发帖"不是一回事。

**后果：**

```js
// js/09:420
let roleId = document.getElementById('userPostRoleSelect')?.value || 'me';
//                    ↑ 恒为 null        ↑ 恒为 undefined      ↑ 所以恒为 'me'
postChar = roleId === 'me' ? currentUser : myCharacters.find(...);
//                                          ↑ 这一支永远走不到
```

- 发出去的推文作者**永远是你自己**，"以某个角色身份发帖"这条分支是死代码
- `js/04:143` 的 `if(selRole)` 判空为假，填充下拉选项那一步整个被跳过
- `js/04:158` 的头像预览也永远显示你自己

因为全程用了 `?.` 和判空，**不报错、不留痕，是完全静默的失效**——所以你可能一直以为这功能只是"没做入口"。

**修法：** 要么在 `index.html` 的发帖弹窗里补一个 `id="userPostRoleSelect"` 的 `<select>`，要么把这三处的 id 改成实际存在的那个。取决于你原本想要哪种行为。

---

### 🟠 2. 移动端顶部标题永远不更新

**位置：** `js/04:25`、`js/07:546`

**问题：** 代码找的是 `mobileTopTitle`，`index.html:129` 里真实的 id 是 **`mtbCenterTitle`**。改名时漏改了。`style.css:464` 还留着给 `#mobileTopTitle` 写的样式，是历史遗留的旁证。

**后果：** 有 `if(mtEl)` 保护所以不报错，但手机上切换页面时本该显示的页面标题（"话题""设置""聊天"……）永远不会出现。`js/07:546` 那处是 `switchMainView()` 的公共路径，**所有页面**都受影响。

**注意：** 即使把 id 改对，`style.css:472-474` 还有一条 `#mtbCenterTitle { display:none !important; }` 在 `@media (max-width:900px)` 里，会把它继续按住。这是两个独立的问题，要一起改才看得到效果。

---

### 🟠 3. 搜索结果里"信件"永远被标成"日记"

**位置：** `js/09:149`

**问题：** 第 106 行是 `['letters', 'diaries'].forEach(...)`，所以 `r.kind` 只可能是**复数**的 `'letters'` / `'diaries'`。但第 149 行写的是：

```js
${r.kind === 'letter' ? '信件' : '日记'}   // ← 单数，永远不成立
```

**旁证：** 同文件 175 行的 `openDiaryFromSearch` 写的是 `kind === 'letters'`（复数，正确）。所以 149 行确实是单复数笔误。

**后果：** 全站搜索的"📔 日记与信件"分类下，命中的信件全被标成"日记"。

---

### 🟠 4. 用 `||` 兜底数字，把用户填的 `0` 吃掉了

**位置：** `js/09:448-451` 和 `:454-457`

```js
postStats.likes = parseInt(likesVal) || getRandomStat(500);
//                 填 0 → parseInt 得 0 → 0 是假值 → 被随机数覆盖
```

**后果：** 你想发一条"0 赞 0 评论 0 转发"的推文时做不到，会被自动换成随机正数。

**旁证：** 同文件 440-443 行的另一条分支写的是 `rtVal !== '' ? parseInt(rtVal) : (...)`，正确保留了 0。所以 448 行这几处是疏漏而非有意。

**修法：** 把 `||` 换成显式判空，例如 `likesVal !== '' ? parseInt(likesVal) : getRandomStat(500)`。

---

### 🟡 5. `__uncategorized__` 哨兵值会泄漏成真分类名

**位置：** `js/12:17`

**问题：** `const defaultCategory = activeWbCategoryFilter || '';`

`activeWbCategoryFilter` 除了真实分类名和 `null`，还有一个特殊值 `'__uncategorized__'`（代表"未分类"这个虚拟筛选项，`js/06:1091` 的注释明确写了它不该写进 `worldbookCategories`）。这里没排除它。

**后果：** 当你在世界书页面把筛选切到"未分类"、然后导入 TXT/JSON 世界书时，新条目会被打上字面量分类 `"__uncategorized__"`。接着 `confirmWbImportAll()`（`js/12:159`）会把它当成真分类名 push 进分类列表——你的分类栏里会多出一个诡异的 `__uncategorized__` 标签。而且这些条目因为 `category` 非空，**反而从"未分类"筛选里消失了**，跟直觉正好相反。

**修法：** `const defaultCategory = (activeWbCategoryFilter && activeWbCategoryFilter !== '__uncategorized__') ? activeWbCategoryFilter : '';`

---

### 🟡 6. 四处 `onclick` 里插了没转义的文本

**位置：** `js/03:1073`（话题标签）、`js/03:1304`（趋势榜标题）、`js/03:1390`（趋势编辑框）、`js/10:1449`（角色势力标签）

**问题：** 都是把可能含引号的文本直接拼进 HTML 属性，例如：

```js
html.replace(/#(\S+)/g, '<span onclick="switchMainView(\'tag\', \'#$1\')">#$1</span>')
```

**后果：** 只要文本里出现一个英文单引号（AI 很容易生成 `#can't`、`#老王's笔记` 这种标签），属性就会被提前截断，这条标签点击失效，严重时连累后面的 HTML 结构错乱。第 1390 行是双引号版本，同理。`js/10:1449` 是势力名（你自己手输的），风险低一些但性质一样。

**旁证：** 同文件的 `renderQuotedPostPreviewHTML`、`generatePostHTML` 里的角色名**都调用了 `escapeHtml`**，所以这四处是遗漏。

**修法：** 显示位置套 `escapeHtml()`，`onclick` 参数位置额外把单引号转义掉（参考 `js/13:683` 已有的 `.replace(/'/g,"\\'")` 写法，那里就是对的）。

---

### 🟢 7. 无害但确实多写了的

- **`js/04:1059`**：`openUserProfileModal()` 里的字段数组把 `'myPersona'` 写了两遍（第 3 项和最后一项）。只是对同一个输入框重复赋同样的值，不影响功能。
- **`enableAffinitySystem`**（`js/04:522/678/752`）：这个 id 确实不存在，但 `js/01:323` 的注释写明好感度系统**已经迁移到插件**、设置项被有意移除了，三处都有判空保护。是重构后的合理死代码，**不是 bug**。
- **`js/05:795`** `charStatusScheduleText` 和它的 `dataset.expanded`：全项目只在这一处被写入，没有任何地方读取或显示。查看日程实际走的是 `toggleScheduleInBubble()` 打开独立弹窗。疑似"气泡内展开日程"这个功能的未完成残留。

---

### ❔ 8. 两条待确认（不确定是不是你故意这么写的）

- **`js/01:1951-1954`** MVU 状态栏的 `insert` 操作：判断条件是 `action === 'insert' && Array.isArray(node[lastKey])`。当某个路径**第一次** insert（此位置还不是数组）时条件不成立，会落进 `else` 直接覆盖赋值，而不是新建数组。结果这个路径永远只能保留最后一次 insert 的值，攒不成列表。**如果**你的预设总会先用别的 op 把数组初始化好，那就没问题。
- **`js/01:1038`** 流式输出：`onDelta(...)` 被包在 `if (deltaText)` 里，纯思维链增量（`deltaText` 为空）不会触发界面刷新。用推理模型时，长思考阶段界面会看起来卡住不动，直到吐出第一个正文字符才一次性刷出来。如果你觉得"思考过程不需要逐字流"，那这就是有意简化。

---

## 三、查过但没问题的部分

这些都跑了脚本或人工核对，结果是干净的，列出来是为了让你知道**已经查过了**：

- ✅ **语法**：17 个 js 模块 + `service-worker.js` 全部通过 `node --check`
- ✅ **重复 id**：`index.html` 里 618 个 id，**零重复**（这个很难得，通常大项目都会有几个）
- ✅ **函数缺失**：`index.html` 内联事件里调用的所有函数，在 js 里都能找到定义，无一遗漏
- ✅ **函数重名覆盖**：838 个函数里只有 `onMove` / `onUp` 重名，但两者都嵌套在各自的外层函数内部（函数作用域隔离），不会互相覆盖——**误报**
- ✅ **`js/12` 的所有 `getElementById`**：全部能在 `index.html` 找到对应元素
- ✅ **`js/14`、`js/15`**：拖拽排序下标运算、深度注入插入位置计算、预设导入导出字段往返、启用状态互斥切换，逐条核对无误
- ✅ **状态气泡设置的那批 id**（`statusTypesList`、`colorPalette`、`newStatusTypeColor` 等 6 个）：都在 `index.html:2138-2146` 真实存在，只是用了单引号写法 `id='xxx'`，是我第一版脚本的误报

---

## 四、本次已经顺手改掉的

| 文件 | 改动 |
|---|---|
| `index.html` | 手机顶栏加 🔄 刷新图标；左侧导航栏加「🔄 刷新」项；插件页加刷新按钮 |
| `js/02-databank-plugins-minigames.js` | 新增 `refreshAppPage()`，带确认弹窗、图标转圈反馈、SW 更新检查（1.5 秒超时兜底） |
| `style.css` | 新增刷新图标的旋转动画 |
| `service-worker.js` | 版本号 v50 → v52；预缓存补上 `localforage.min.js`（**这个最关键，之前漏了它离线直接打不开**）、`mammoth.browser.min.js`、`16-story-studio.js`、`17-tavern-bridge.js`；`manifest.json` 路径纠正为 `pwa-manifest.json`。现在 `index.html` 引用的 21 个资源**全部**在清单里 |

**另外：** `js/vendor/jquery.min.js`（87 KB）全项目搜不到任何引用，是个没人用的文件，可以删。
（注：`js/03` 里的角色卡 iframe 会在卡片脚本用到 `$` 时按需注入 jQuery，但走的是自己的注入逻辑，不依赖这个文件。删之前建议再确认一下。）

---

## 五、建议的修复顺序

1. **删 `manifest.json`** —— 一秒钟的事，但不删下次打包还是白屏
2. **修 `userPostRoleSelect`** —— 唯一一个"功能完全没了"的问题
3. **`__guyuBooted` 挪进 `finally`** —— 手机上出问题时能少踩一个大坑
4. 剩下的（标题 id、单复数、`||` 吞 0、哨兵值、转义）都是"偶尔出洋相"级别，有空再收拾

需要我直接动手改哪几条，说一声就行。
